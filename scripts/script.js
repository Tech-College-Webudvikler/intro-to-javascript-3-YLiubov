// let emailRegex = /^\S+@\S+\.\S+$/

// 🟦 Упражнение 3.1 – Событие Submit
// Тебе нужно сделать валидацию формы регистрации. HTML-форма уже написана, твоя задача – взять значения, которые пользователь вводит, и проверить, правильные ли они.
// После этого в элемент <p> с id="userMessage" должна появляться строка с обратной связью для пользователя.
// Требования:
// Объяви переменную для формы и получи её через getElementById.
// Добавь обработчик событий, который слушает событие "input".
// Проверь, что email введён правильно (используй готовый regex).
// Проверь, что пароль минимум 8 символов.
// Проверь, что значение во втором поле пароля совпадает с первым.
// Если пользователь что-то ввёл неправильно – покажи соответствующую ошибку в DOM.
// Если всё ок – покажи сообщение, что пользователь успешно зарегистрирован.

const form = document.getElementById("signup"); // 1
const userMessage = document.getElementById("userMessage"); // 2

form.addEventListener("submit", (event) => { // 3
    event.preventDefault(); // 4

    const email = form.emailField.value; // 5
    const password = form.passwordField.value; // 6
    const passwordRepeat = form.passwordRepeatField.value; // 7
    
    let emailRegex = /^\S+@\S+\.\S+$/; // 8

    if (!emailRegex.test(email)) { // 9
        userMessage.textContent = "Invalid email"
    } else if (password.length < 8) { // 10
        userMessage.textContent = "The password must be more than 8 characters long"
    } else if (password !== passwordRepeat) { // 11
        userMessage.textContent = "Password does not match"
    } else {
        userMessage.textContent = "Registration successful"
    }
});

// Пояснения
// 	1.	Находим форму по id.
// 	2.	Находим параграф для сообщений.
// 	3.	Добавляем обработчик события submit.
// 	4.	Останавливаем стандартное поведение (чтобы страница не перезагружалась).
//     5–7. Берём значения из полей формы.
// 	5.	Берём текст из поля email (у него name="emailField").
// 	6.	Если email неверный → показываем ошибку.
// 	7.	Если пароль короче 8 символов → ошибка.
// 	8.	Это шаблон (регулярное выражение) для проверки, что email выглядит корректно.
// 	9.  Проверяем email: если не подходит под шаблон — выводим «Неверный email».
//  10. Проверяем пароль: если меньше 8 символов — пишем, что он слишком короткий.
//  11. Проверяем совпадение паролей: если не совпадают — пишем, что они разные.
//  12. Если все проверки пройдены — показываем сообщение об успешной регистрации.

// ___________________________________________________________________________________________

// 🟦 Øvelse 3.2 – Кнопка-счётчик
// У тебя есть кнопка с id="clickCount".
// Что нужно сделать в JS:
// Найти кнопку.
// Создать переменную count = 0 в глобальной области.
// Добавить обработчик события click.
// Каждый раз при клике увеличивать count на 1.
// Обновлять текст кнопки, чтобы там было:
// «Du har klikket X gange» (где X — число кликов).

let count = 0; // (1)
const button = document.getElementById("clickCount"); // (2)

    button.addEventListener("click", () => { // (3)
        count++; // (4)
    button.innerText = "Du har klikket " + count + " gange"; // (5)
});

//  1.	Глобальная переменная-счётчик (начинается с 0).
// 	2.	Находим кнопку по id.
// 	3.	Вешаем обработчик клика.
// 	4.	Прибавляем +1 к счётчику.
// 	5.	Обновляем текст кнопки.

// ___________________________________________________________________________________________

